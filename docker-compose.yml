version: '3.8'

services:
  bad-data:
    build: .
    container_name: bad-data-party-game
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST_IP=${HOST_IP:-}
      - BASE_URL=${BASE_URL:-}
      - PIPER_TTS_URL=http://piper-tts:5000
    volumes:
      - ./data:/app/data
    depends_on:
      - piper-tts
    restart: unless-stopped

  # Piper Neural Text-To-Speech (Expressive Game Host Voice)
  piper-tts:
    image: rhasspy/piper-http:latest
    container_name: bad-data-piper-tts
    ports:
      - "5000:5000"
    command: --voice ${PIPER_VOICE:-en_US-ryan-high}
    restart: unless-stopped
